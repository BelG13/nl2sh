[project]
name = 'nl2sh'
version = '0.1.0'
description = 'nl2sh tool using small language models.'
authors = ["Giovanni Belval Giovanni.belval@icloud.com"]
licence = { file = "LICENCE" }
requires-python = '>=3.12'
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "transformers",
    "torch"
]
